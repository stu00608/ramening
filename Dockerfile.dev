FROM node:20-alpine

# 設定工作目錄
WORKDIR /app

# 安裝系統依賴
RUN apk add --no-cache libc6-compat

# 複製 package.json 和 package-lock.json
COPY package*.json ./

# 安裝依賴
RUN npm ci

# 複製專案檔案
COPY . .

# 產生 Prisma 客戶端
RUN npx prisma generate

# 暴露端口
EXPOSE 3000

# 啟動開發伺服器
CMD ["npm", "run", "dev"]